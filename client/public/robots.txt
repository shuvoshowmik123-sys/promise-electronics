# robots.txt for promiseelectronics.com
# Promise Electronics - TV Repair & Electronics Service

User-agent: *
Allow: /

# Explicitly allow favicon for Google Search results
Allow: /favicon.png
Allow: /favicon.ico

# Allow search engines to crawl all public pages
Allow: /shop
Allow: /repair
Allow: /about
Allow: /track-order
Allow: /track

# Block admin and private areas
Disallow: /admin
Disallow: /admin/*
Disallow: /api/
Disallow: /checkout
Disallow: /my-profile

# GOOGLE-OAUTH-BRANDING: /privacy-policy and /terms-and-conditions are React
# pages that load their text from /api/policies/* at runtime. With /api/ blocked
# above, a crawler that renders JavaScript still could not fetch that content —
# so both legal pages were unreadable to Google in every mode. These two
# endpoints are public, read-only policy text and are safe to expose.
# Allow must precede the broader Disallow for longest-match to apply.
# TO REVERT: delete the two Allow lines below.
Allow: /api/policies/privacy
Allow: /api/policies/terms

# Block development/utility pages
Disallow: /*.json$
Disallow: /*?*

# Crawl delay for politeness
Crawl-delay: 1

# Sitemap location
Sitemap: https://www.promiseelectronics.com/sitemap.xml

# Google specific
User-agent: Googlebot
Allow: /
Crawl-delay: 0

# Bing specific
User-agent: Bingbot
Allow: /
Crawl-delay: 1
